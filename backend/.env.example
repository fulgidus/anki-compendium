# Environment
ENVIRONMENT=development
DEBUG=true

# Database
DATABASE_URL=postgresql+asyncpg://ankiuser:changeme@localhost:5432/anki_compendium_dev

# CORS (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Security
SECRET_KEY=change-this-in-production-use-openssl-rand-hex-32

# Google Gemini AI Configuration
# Required for RAG pipeline - Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
# LangChain uses GOOGLE_API_KEY by default - we set it automatically in code
# GOOGLE_API_KEY will be set from GEMINI_API_KEY at runtime

# MinIO S3 Storage
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme
MINIO_USE_SSL=false

# n8n Workflow Automation
# Base URL for n8n webhook triggers (used for PDF processing workflows)
N8N_WEBHOOK_URL=http://localhost:5678
N8N_WEBHOOK_SECRET=change-this-secret-in-production

# RabbitMQ - DEPRECATED: Using n8n workflows instead
# RABBITMQ_URL=amqp://admin:changeme@localhost:5672/

# Keycloak (for authentication)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=anki-compendium
KEYCLOAK_CLIENT_ID=anki-api
KEYCLOAK_CLIENT_SECRET=change-me
